# ==============================
# SERVER
# ==============================
server.port=8080

# ==============================
# DATABASE (PostgreSQL)
# ==============================
# Render will inject these environment variables automatically.
# If not present, Spring Boot will use the local PostgreSQL defaults.

SPRING_DATASOURCE_URL=jdbc:postgresql://dpg-d6rf7osr85hc73fbhjf0-a:5432/smart_attendance_y531
SPRING_DATASOURCE_USERNAME=smart_user
SPRING_DATASOURCE_PASSWORD=gswM8LNE4yxmmn7pzLDaldETHZoLBU8x
spring.datasource.driver-class-name=org.postgresql.Driver

# ==============================
# JPA / HIBERNATE
# ==============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

# ==============================
# JWT CONFIG
# ==============================
jwt.secret=SmartAttendanceIoTPlatformSuperSecretKeyForJWTTokenGeneration2024!@#$%^&*
jwt.expiration=86400000

# ==============================
# FILE UPLOAD
# ==============================
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB

# ==============================
# CORS
# ==============================
# Render will automatically replace this with the frontend URL
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173}